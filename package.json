{
  "name": "fullstack2020_node",
  "version": "1.0.0",
  "description": "Fullstack 2020 backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd C:/Users/Nikke/Documents/FullStack2020/osa2/puhelinluettelo && npm run build --prod && cp -r build C:/Users/Nikke/Documents/fullstack2020_node",
    "deploy": "git push heroku master",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",    
    "logs:prod": "heroku logs --tail"
  },
  "author": "Niklas Rönnholm",
  "license": "GPL-3.0",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
